version: "3.9"

services:

  app-v1:
    build: ./app
    container_name: app-v1
    environment:
      APP_VERSION: v1
    ports:
      - "3001:3000"

  app-v2:
    build: ./app
    container_name: app-v2
    environment:
      APP_VERSION: v2
    ports:
      - "3002:3000"