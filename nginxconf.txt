events {}

http {

    upstream backend {
        server app-v1:3000;
        server app-v2:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }

        location /health {
            proxy_pass http://backend/health;
        }
    }
}